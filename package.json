{
  "name": "cadence",
  "private": true,
  "version": "0.6.8-work",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:cloudflare": "npm test && npm run build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "verify": "npm test && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
